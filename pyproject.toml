[project]
name = "spark-workshop"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.1",
    "pyspark>=4.1.1",
]

[dependency-groups]
dev = [
    "jupyter>=1.1.1",
]
